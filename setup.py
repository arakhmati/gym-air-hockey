from setuptools import setup

setup(name='gym_air_hockey',
      version='0.0.1',
      install_requires=['gym',
                        'pygame',
                        'numpy',
                        'cv2']
)